pipeline {
  agent     { label 'jenkins_agent' }
  //environment { MACHINE_IP=buildUtils.ip(dscr:true) }
  stages { 
    stage('Checkout from GITHUB') { 
      steps {
        script {
          sh "pwd"
          sh "ls -l"
          sh "python"
        }
      }
    }    
  }
}